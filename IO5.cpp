#include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>
#include <set>
using namespace std;

void Divisibility() {
    
}

int main() {
      
    int N = 0;
    cin>>N;
    
    long data[N];
    for(auto i=0; i<N; i++)
        cin>>data[i];
    
        // while (num>=0)
        // {
        //     int rem = arr[num-1] %10;
        // }    
    // write your code here

int res = data[N-1]%10;    // only need to check last number  

if(res == 0){ cout<<"Yes"; }

else{ cout<<"No"; }  


    ans = 
    
    
    cout<<ans;
    
}