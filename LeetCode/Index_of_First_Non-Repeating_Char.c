#include<stdio.h>
#include<stdlib.h>


struct Node
{
    int data;
    struct Node* next;
    /* data */
};

void class LinkedList
{

}
// int IOFNRChar(char str[])
// {
    
//  for (auto i : s) {
//         if (count(s.begin(), s.end(), i) == 1) {
//             fnc = i;
//             break;
//         }
//         else {
//             index += 1;
//         }
//     }
//}

// void func(){}

// int main()
// {
// char string[] = "abcdeacf";  // string is a char array in C
// printf("%s\n", string);
// printf("Hello World \n \r");
// printf("%d %f %c %s %u %ld");

// return 0;
// }















// #include <string.h>

// inline void swap(char *s, int i, int j)
// {
//     char tmp = s[i];
//     s[i] = s[j];
//     s[j] = tmp;
// }


// bool areAlmostEqual(char * s1, char * s2){
    
//     int len1 = strlen(s1);
//     int len2 = strlen(s2);
//     char tmp[101];
    
//     if (!strcmp(s1, s2))
//         return true;
    
//     for (int i=0; i < len1-1; i++) {
        
//         for (int j=i+1; j < len2; j++ ) {
//             strcpy(tmp, s1);
//             swap(tmp, i, j);
//             if (!strcmp(tmp, s2))
//                 return true;
//         }
//     }
//     return false;
// }

// Started with basic pointer based questions, whats and hows of pointers, difference between 

// 1. wild and dangling pointer etc.
// friend class, What are templates in cpp
// What are inline functions
// Define inheritance
//Polymorphism? Demonstrate inheritance with method overriding.
// 2. Asked me the syntax to allocated memory dynamically in C, (basically using malloc and realloc)

// 3. Asked about RAID(0,1,2) 
// 4. Microprocessors
// 5. CISC vs RISC, 
// 6. volatile keyword, 
// 7. memory allocation differences in C and C++....

// Asked me to write pseudo-code for a basic parser that'd 
// 8. parse the code into an AST (Only telling him the approach was enough)

// CPU Sheduling bottle necks , 
// can we call mutex a binary semaphore? if not then why not ? 
// virtualisation of memory and difference between MMU and CPU.

// Questions related to bootstrapping of operating system,
// A-Z of the stuff that happens to a computer when a peripheral is connected.
// interrupts and timers

// Finally he asked me to write working code of inserting a node in a double linked list after a given index.
//Asked about localisation, stack and heap memory, where will a variable be stored if it's uninitialized, Windows uses big or little endian etc

